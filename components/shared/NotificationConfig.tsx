'use client';

import { notification } from 'antd';

notification.config({
  placement: 'bottomRight',
  duration: 4,
  maxCount: 3,
});

export default function NotificationConfig() {
  return null;
}